add_library(PersonalToolsWindowsShared STATIC Shared.cpp)
target_link_libraries(PersonalToolsWindowsShared PUBLIC PersonalToolsCore ole32 shell32 uuid advapi32)
target_include_directories(PersonalToolsWindowsShared PUBLIC "${CMAKE_CURRENT_SOURCE_DIR}")
target_compile_definitions(PersonalToolsWindowsShared PUBLIC UNICODE _UNICODE NOMINMAX WIN32_LEAN_AND_MEAN _WIN32_WINNT=0x0A00)

add_library(PersonalToolsAPO SHARED Apo.cpp Apo.def)
target_link_libraries(PersonalToolsAPO PRIVATE PersonalToolsWindowsShared)
set_target_properties(PersonalToolsAPO PROPERTIES PREFIX "")

add_library(PersonalToolsBridge SHARED UiBridge.cpp)
target_link_libraries(PersonalToolsBridge PRIVATE PersonalToolsWindowsShared)
set_target_properties(PersonalToolsBridge PROPERTIES PREFIX "")
add_executable(PersonalToolsSetup WIN32 Setup.cpp)
target_compile_definitions(PersonalToolsSetup PRIVATE UNICODE _UNICODE NOMINMAX WIN32_LEAN_AND_MEAN)
target_link_libraries(PersonalToolsSetup PRIVATE ole32 shell32)
if(MINGW)
    target_link_options(PersonalToolsSetup PRIVATE -municode -static)
    target_link_options(PersonalToolsAPO PRIVATE -static-libgcc -static-libstdc++ -static)
    target_link_options(PersonalToolsBridge PRIVATE -static)
endif()
if(MSVC)
    target_compile_options(PersonalToolsAPO PRIVATE /W4 /permissive- /EHsc)
    target_compile_options(PersonalToolsBridge PRIVATE /W4 /permissive- /utf-8 /EHsc)
    target_compile_options(PersonalToolsSetup PRIVATE /W4 /permissive- /EHsc)
endif()
install(TARGETS PersonalToolsSetup PersonalToolsAPO RUNTIME DESTINATION . LIBRARY DESTINATION .)
install(TARGETS PersonalToolsBridge RUNTIME DESTINATION ui LIBRARY DESTINATION ui)
install(FILES "${PROJECT_SOURCE_DIR}/Scripts/install-windows.ps1" "${PROJECT_SOURCE_DIR}/Scripts/uninstall-windows.ps1" "${PROJECT_SOURCE_DIR}/Scripts/WindowsInstall.psm1" DESTINATION .)
install(FILES "${PROJECT_SOURCE_DIR}/Scripts/WindowsDeviceRecovery.psm1" "${PROJECT_SOURCE_DIR}/Scripts/repair-windows-device.ps1" "${PROJECT_SOURCE_DIR}/Scripts/register-windows-device-recovery.ps1" DESTINATION .)
install(FILES "${PROJECT_SOURCE_DIR}/Scripts/repair-windows-capture.ps1" DESTINATION .)
add_test(NAME PersonalToolsDeviceRecoveryTests COMMAND powershell.exe -NoProfile -ExecutionPolicy Bypass -File "${PROJECT_SOURCE_DIR}/Tests/WindowsDeviceRecoveryTests.ps1")

add_executable(PersonalToolsApoTests "${PROJECT_SOURCE_DIR}/Tests/WindowsApoTests.cpp" Shared.cpp)
target_include_directories(PersonalToolsApoTests PRIVATE "${CMAKE_CURRENT_SOURCE_DIR}")
target_compile_definitions(PersonalToolsApoTests PRIVATE PERSONALTOOLS_TESTING UNICODE _UNICODE NOMINMAX WIN32_LEAN_AND_MEAN _WIN32_WINNT=0x0A00)
target_link_libraries(PersonalToolsApoTests PRIVATE PersonalToolsCore ole32 shell32 uuid advapi32)
if(MINGW)
    target_link_options(PersonalToolsApoTests PRIVATE -static)
endif()
add_test(NAME PersonalToolsApoTests COMMAND PersonalToolsApoTests)

add_dependencies(PersonalToolsApoTests PersonalToolsAPO)

add_executable(PersonalToolsUiBridgeTests "../../Tests/WindowsUiBridgeTests.cpp" Shared.cpp)
target_include_directories(PersonalToolsUiBridgeTests PRIVATE .)
target_compile_definitions(PersonalToolsUiBridgeTests PRIVATE PERSONALTOOLS_TESTING UNICODE _UNICODE NOMINMAX WIN32_LEAN_AND_MEAN _WIN32_WINNT=0x0A00)
target_link_libraries(PersonalToolsUiBridgeTests PRIVATE PersonalToolsCore ole32 shell32 uuid advapi32)
if(MSVC)
    target_compile_options(PersonalToolsUiBridgeTests PRIVATE /utf-8 /EHsc)
endif()
if(MINGW)
    target_link_options(PersonalToolsUiBridgeTests PRIVATE -static)
endif()
add_test(NAME PersonalToolsUiBridgeTests COMMAND PersonalToolsUiBridgeTests)

add_executable(PersonalToolsNeuralTests "../../Tests/WindowsNeuralTests.cpp")
target_include_directories(PersonalToolsNeuralTests PRIVATE .)
target_compile_definitions(PersonalToolsNeuralTests PRIVATE UNICODE _UNICODE NOMINMAX WIN32_LEAN_AND_MEAN)
target_link_libraries(PersonalToolsNeuralTests PRIVATE PersonalToolsCore)
if(MINGW)
    target_link_options(PersonalToolsNeuralTests PRIVATE -static)
endif()
add_test(NAME PersonalToolsNeuralTests COMMAND PersonalToolsNeuralTests)

add_executable(PersonalToolsNeuralReplay "../../Tests/WindowsNeuralReplay.cpp")
target_include_directories(PersonalToolsNeuralReplay PRIVATE .)
target_compile_definitions(PersonalToolsNeuralReplay PRIVATE NOMINMAX)
target_link_libraries(PersonalToolsNeuralReplay PRIVATE PersonalToolsCore)
if(MINGW)
    target_link_options(PersonalToolsNeuralReplay PRIVATE -static)
endif()

install(FILES "${PROJECT_SOURCE_DIR}/docs/windows.md" "${PROJECT_SOURCE_DIR}/docs/windows-validation.md" DESTINATION .)
install(FILES "${PROJECT_SOURCE_DIR}/docs/antigravity-bridge.md" DESTINATION .)
