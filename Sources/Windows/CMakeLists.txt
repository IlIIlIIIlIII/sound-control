add_library(SoundControlWindowsShared STATIC Shared.cpp)
target_link_libraries(SoundControlWindowsShared PUBLIC SoundControlCore ole32 shell32 uuid advapi32)
target_include_directories(SoundControlWindowsShared PUBLIC "${CMAKE_CURRENT_SOURCE_DIR}")
target_compile_definitions(SoundControlWindowsShared PUBLIC UNICODE _UNICODE NOMINMAX WIN32_LEAN_AND_MEAN _WIN32_WINNT=0x0A00)

add_library(SoundControlAPO SHARED Apo.cpp Apo.def)
target_link_libraries(SoundControlAPO PRIVATE SoundControlWindowsShared)
set_target_properties(SoundControlAPO PROPERTIES PREFIX "")

add_library(SoundControlBridge SHARED UiBridge.cpp)
target_link_libraries(SoundControlBridge PRIVATE SoundControlWindowsShared)
set_target_properties(SoundControlBridge PROPERTIES PREFIX "")
add_executable(SoundControlSetup WIN32 Setup.cpp)
target_compile_definitions(SoundControlSetup PRIVATE UNICODE _UNICODE NOMINMAX WIN32_LEAN_AND_MEAN)
target_link_libraries(SoundControlSetup PRIVATE ole32 shell32)
if(MINGW)
    target_link_options(SoundControlSetup PRIVATE -municode -static)
    target_link_options(SoundControlAPO PRIVATE -static-libgcc -static-libstdc++ -static)
    target_link_options(SoundControlBridge PRIVATE -static)
endif()
if(MSVC)
    target_compile_options(SoundControlAPO PRIVATE /W4 /permissive- /EHsc)
    target_compile_options(SoundControlBridge PRIVATE /W4 /permissive- /utf-8 /EHsc)
    target_compile_options(SoundControlSetup PRIVATE /W4 /permissive- /EHsc)
endif()
install(TARGETS SoundControlSetup SoundControlAPO RUNTIME DESTINATION . LIBRARY DESTINATION .)
install(TARGETS SoundControlBridge RUNTIME DESTINATION ui LIBRARY DESTINATION ui)
install(FILES "${PROJECT_SOURCE_DIR}/Scripts/install-windows.ps1" "${PROJECT_SOURCE_DIR}/Scripts/uninstall-windows.ps1" "${PROJECT_SOURCE_DIR}/Scripts/WindowsInstall.psm1" DESTINATION .)
install(FILES "${PROJECT_SOURCE_DIR}/Scripts/WindowsDeviceRecovery.psm1" "${PROJECT_SOURCE_DIR}/Scripts/repair-windows-device.ps1" "${PROJECT_SOURCE_DIR}/Scripts/register-windows-device-recovery.ps1" DESTINATION .)
install(FILES "${PROJECT_SOURCE_DIR}/Scripts/repair-windows-capture.ps1" DESTINATION .)
add_test(NAME SoundControlDeviceRecoveryTests COMMAND powershell.exe -NoProfile -ExecutionPolicy Bypass -File "${PROJECT_SOURCE_DIR}/Tests/WindowsDeviceRecoveryTests.ps1")

add_executable(SoundControlApoTests "${PROJECT_SOURCE_DIR}/Tests/WindowsApoTests.cpp" Shared.cpp)
target_include_directories(SoundControlApoTests PRIVATE "${CMAKE_CURRENT_SOURCE_DIR}")
target_compile_definitions(SoundControlApoTests PRIVATE SOUNDCONTROL_TESTING UNICODE _UNICODE NOMINMAX WIN32_LEAN_AND_MEAN _WIN32_WINNT=0x0A00)
target_link_libraries(SoundControlApoTests PRIVATE SoundControlCore ole32 shell32 uuid advapi32)
if(MINGW)
    target_link_options(SoundControlApoTests PRIVATE -static)
endif()
add_test(NAME SoundControlApoTests COMMAND SoundControlApoTests)

add_dependencies(SoundControlApoTests SoundControlAPO)

add_executable(SoundControlUiBridgeTests "../../Tests/WindowsUiBridgeTests.cpp" Shared.cpp)
target_include_directories(SoundControlUiBridgeTests PRIVATE .)
target_compile_definitions(SoundControlUiBridgeTests PRIVATE SOUNDCONTROL_TESTING UNICODE _UNICODE NOMINMAX WIN32_LEAN_AND_MEAN _WIN32_WINNT=0x0A00)
target_link_libraries(SoundControlUiBridgeTests PRIVATE SoundControlCore ole32 shell32 uuid advapi32)
if(MSVC)
    target_compile_options(SoundControlUiBridgeTests PRIVATE /utf-8 /EHsc)
endif()
if(MINGW)
    target_link_options(SoundControlUiBridgeTests PRIVATE -static)
endif()
add_test(NAME SoundControlUiBridgeTests COMMAND SoundControlUiBridgeTests)

add_executable(SoundControlNeuralTests "../../Tests/WindowsNeuralTests.cpp")
target_include_directories(SoundControlNeuralTests PRIVATE .)
target_compile_definitions(SoundControlNeuralTests PRIVATE UNICODE _UNICODE NOMINMAX WIN32_LEAN_AND_MEAN)
target_link_libraries(SoundControlNeuralTests PRIVATE SoundControlCore)
if(MINGW)
    target_link_options(SoundControlNeuralTests PRIVATE -static)
endif()
add_test(NAME SoundControlNeuralTests COMMAND SoundControlNeuralTests)

add_executable(SoundControlNeuralReplay "../../Tests/WindowsNeuralReplay.cpp")
target_include_directories(SoundControlNeuralReplay PRIVATE .)
target_compile_definitions(SoundControlNeuralReplay PRIVATE NOMINMAX)
target_link_libraries(SoundControlNeuralReplay PRIVATE SoundControlCore)
if(MINGW)
    target_link_options(SoundControlNeuralReplay PRIVATE -static)
endif()

install(FILES "${PROJECT_SOURCE_DIR}/docs/windows.md" "${PROJECT_SOURCE_DIR}/docs/windows-validation.md" DESTINATION .)
install(FILES "${PROJECT_SOURCE_DIR}/docs/antigravity-bridge.md" DESTINATION .)
