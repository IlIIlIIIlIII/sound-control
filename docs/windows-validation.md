# Windows 이식 검증 기록

## 2026-09-21: SMSL endpoint 변경 후 자동 복구

- 원인: 설정은 연결이 끊긴 `스피커(3- SMSL USB DAC)` / `43406fc5-af54-479b-b33d-846022c5cbec`를 가리켰고, 활성 출력은 `스피커(5- SMSL USB DAC)` / `8854650b-81fd-436c-bcb9-cfc59f893b9c`였다. AEC/NPU는 켜져 있으나 참조가 없어 원음을 전달했다. 새 출력에는 EQ 등록도 없었다.
- 실제 PnP 부모 정보를 확인했다. SMSL은 `USB\VID_152A&PID_85DD`, USB capabilities `132`로 UNIQUEID 플래그가 없고 Container ID도 바뀌었다. 위치 기반 USB instance suffix를 시리얼로 취급하지 않는다. 같은 기능/하드웨어의 활성 출력 후보가 하나여서 `unique-usb-model` 방식으로 복구했다. 이 방식은 물리적 개체를 고유하게 증명하지 않으며 동일 모델 복수 후보는 보류한다. MOTU는 별도로 실제 UNIQUEID 플래그와 시리얼이 있음을 확인했다.
- 관리자 소유 설치 복원 파일에 식별 정보와 새 출력의 원래 효과 값을 추가했다. 설정 비교 후 갱신하며, 필터와 EQ/AEC 토글은 보존한다. 진행 중 복구는 기록을 남겨 재시도/제거가 가능하다. 출력 복구만 구현했으며 마이크 endpoint 자동 교체는 포함하지 않는다.
- `SoundControl Device Recovery` SYSTEM 작업을 부팅/1분 주기로 설치했다. 상태 변경 없는 작업 실행의 LastTaskResult=0을 확인했다. 정상 연결에서는 재시작하지 않았으며 audiodg 시작 시각은 22:23:29로 유지됐다. 실제 물리적 USB 분리/재삽입 시험은 하지 않았다. 이미 발생한 endpoint 변경을 복구한 실제 통합 검증이다.
- 전체 CTest **7/7**, 설치 복원 PowerShell 테스트 통과. 새 테스트는 단일 후보, 중복 후보 보류, 시리얼 불일치 거부, 다른 오디오 기능 거부, USB 허브 식별자 사용 방지, 원자적 복원 기록 교체, 기존 OEM 효과 보존을 검증한다. 브리지 테스트는 stale revision/장치 선택 충돌 거부와 토글/필터 보존 및 재시도를 검증했다.
- 설치 후 기존 마이크, 원음 대용 MOTU Mix, 새 SMSL 루프백, MOTU 루프백을 30초간 측정했다. 오디오는 저장하지 않았다. 22:24:06 앱 로그에서 EQ 44.1kHz, AEC 48kHz 및 `DTLN-AEC 256 · NPU 처리 중`을 확인했다. 별도 18초 안정성 표본에서 참조=1, 오류=0, NPU 블록 11585→13848, 누락 카운터 1690434로 일정했다. 전체 카운터는 여러 마이크 인스턴스가 공유하므로 서로 다른 인스턴스 사이의 값을 직접 비교하지 않는다.
- APO SHA256 `6342EA4BA01D028DADADF1CAF7E7C36B3AACBB5C884CF0A1CB9071C3AA0B1DAB` 유지. APO/NPU 모델은 변경하지 않고 설정 브리지·설정 도구·UI·복구 스크립트만 갱신했다. 이전 앱 파일은 `C:\Program Files\SoundControl\device-recovery-backup-20260921-222305`에 보존했다.
- 실제 음성/음악의 정답 분리가 없는 측정이므로 총 RMS 감소를 순수 반향 ERLE나 음성 보존 품질로 해석하지 않는다. 측정 결과는 `measurements/device-recovery-live-2026-09-21.csv`, `measurements/device-recovery-stability-2026-09-21.csv`에 있다.

## 2026-09-20 최종: 이 PC의 실제 시스템 AEC 동작 확인

- MOTU In 1-2의 LFX 등록 및 SMSL 루프백 호환 경로를 실제 설치했다. 독립 진단 출력이 아니라 Windows 공유 모드 클라이언트가 받는 마이크 출력에서 확인했다. 설치 DLL SHA-256: `295E6B0EC6C993861A1910388C658AD904880FF398BBF23B559F9B028927C2E3`.
- 최초 호환 설치에서는 첫 APO가 만든 공유 메모리를 후속 APO가 `CreateFileMapping`으로 다시 열면서 접근 거절이 발생했다. 기존 객체를 `OpenFileMapping(FILE_MAP_READ | FILE_MAP_WRITE)`으로 먼저 열도록 수정했다. 장치/폴더 ACL이나 시스템 보안 설정을 추가로 완화하지 않았다. 제한된 권한의 격리된 테스트 객체로 기존 실패와 수정 성공을 재현했다. 발견용 인스턴스가 실제 스트림으로 전환될 때도 제어 스레드의 LockForProcess에서 참조 연결을 확보한다.
- 실제 여러 마이크 스트림의 참조 연결 성공, 48kHz 처리, 음악 재생 중 참조 활성/음량 감소를 확인했다. 전체 CTest **5/5 통과**. 처음 전체 테스트 재실행에서는 컴파일러 런타임 PATH 누락으로 실행 오류가 발생했고, 올바른 개발 런타임 PATH로 재실행하여 통과했다. 설치 DLL/앱은 해당 개발 런타임 PATH에 의존하지 않는다.
- 사용자가 아이패드의 약 30초 간격 음성 재생이 계속 켜져 있음을 확인했다. Computer Use로 Spotify를 재생/정지했다. 최종 비교는 150초, 149개의 1초 측정 행이다. 오디오는 저장하지 않았다.
- 동시에 네 경로를 캡처했다: MOTU Loopback Mix(처리 전 마이크 대용), 처리된 In 1-2, SMSL 루프백, MOTU Loopback. MOTU 출력 루프백은 전체 측정에서 무음(-200dBFS 바닥값)이므로 Mix에 PC 재생이 섞이지 않았다. 음악이 없을 때 Mix가 마이크보다 6.02dB 낮음을 확인하여 `Mix RMS + 6.0206dB - 처리된 RMS`로 비교했다. 기본 입력 장치는 In 1-2로 유지했으며 Mix는 진단용으로만 열었다.
- 음악 구간: 약 50~137초. 처음 3초를 제외하고 SMSL 참조 > -60dBFS인 **84개 구간**의 전체 입력/출력 RMS 차이 중앙값은 **6.63dB**, 범위 **-1.86~33.88dB**였다. 아이패드 음성과 음악이 섞인 값이며 순수 반향 ERLE로 해석하지 않는다.
- 음악 없는 음성 구간: SMSL 참조 < -100dBFS, 원음 Mix > -45dBFS, 시작 후 2초 초과인 **37개 구간**. 원음 대비 음량 차이 중앙값 **0.00dB**, 범위 **-0.37~0.44dB**로 음성 음량 보존을 확인했다. 음악과 음성이 겹치는 구간의 음성 명료도/단어 보존은 별도 깨끗한 음성 기준 신호나 청취 평가가 없어 정량 확정하지 않았다.
- 결과: `measurements/aec-2026-09-20-system-paired.csv`(동시 입출력 비교), `measurements/aec-2026-09-20-system-meters.csv`(앞선 시스템 콜백 측정). 측정 중 일부 과도 입력에서 클리핑이 관찰되어 모든 조건의 무왜곡을 보장하지 않는다.
- 종료 상태: Spotify 일시 정지, EQ/AEC 켜짐, SoundControl 데스크톱 앱 실행 유지. 이 호환 방식은 앱이 백그라운드에서 스피커 참조를 제공해야 한다. 앱 완전 종료/참조 중단 시 지연된 마이크 원음으로 통과한다. 재부팅 후 자동 실행과 실제 통화 앱별 검증은 이번 측정 범위에 포함하지 않았다.

MOTU Mix 경로의 의미: [M Series 공식 설명서](https://cdn-data.motu.com/manuals/usb-c-audio/M_Series_User_Guide.pdf). 아래 항목들은 수정 과정의 과거 상태다.

## 2026-09-20 후속: MOTU의 구형 효과 경로 확인 및 호환 참조 구현

- v2 초기화 수정 DLL을 관리자 승인 후 실제 설치하고 Windows Audio/AudioEndpointBuilder를 재시작했다. 이후에도 MOTU의 MFX는 생성되지 않았다. SFX 선언 및 투명 SFX 클래스 시험도 실패하여 모두 복원했고, 불필요한 시험 클래스 코드는 제거했다.
- Computer Use로 MOTU의 오디오 향상 설정을 껐다 켰지만 MFX는 호출되지 않았다. LFX 슬롯 시험과 입력 장치 사용 안 함→사용을 수행하자 실제 audiodg에서 AEC 클래스 생성·초기화 성공, 48kHz 처리 콜백을 확인했다. 같은 장치를 MFX로 복원하여 재활성화하면 다시 콜백이 없었다. 구형 슬롯의 참조 입력은 비활성이므로 이것만으로 반향 제거 성공은 아니다.
- 호환 구현: 데스크톱 브리지의 별도 스레드가 선택된 SMSL의 WASAPI 루프백을 48kHz 스테레오로 받고, 페이지 파일 기반 공유 메모리로 마이크 APO에 전달한다. 마이크 원음은 IPC에 넣지 않는다. 공유 메모리 접근은 기존 설정 파일의 DACL을 사용한다. 각 APO의 독립 읽기 위치, 슬롯 덮어쓰기 검출, 단일 작성자, 설정 버전과 QPC 시각 검증을 적용했다. 오디오 콜백에서 파일/장치 열기, 메모리 할당, 잠금 대기를 하지 않는다.
- 구형 연결 속성에 QPC가 없으면 콜백 시각과 프레임 길이로 첫 프레임 시각을 추정한다. 이는 하드웨어 타임스탬프보다 부정확하며 실제 반향 감소 검증이 필요하다. 호환 방식에서는 SoundControl 앱이 백그라운드에서 실행되어야 한다. 앱/참조가 없으면 지연된 마이크 원음을 전달한다.
- 전체 CTest 5/5 및 변경 후 APO/브리지 계약 테스트 통과. IPC의 두 채널/시각 전달, 여러 소비자, 한 작성자, 넘침, 작성자 재연결, 마지막 핸들 종료 후 매핑 해제를 추가 검증했다.
- 호환 DLL/브리지와 되돌리기 정보를 준비하고 관리자 적용 요청을 띄웠다. **이 문서 작성 시 호환 구현의 실제 시스템 적용과 반향 감소 측정은 아직 대기 중이다.** 최신 실행 결과는 아래 과거 기록과 구분해야 한다.

## 2026-09-20: Spotify / MOTU 실제 AEC 진단 — 시스템 적용 미완료

- Computer Use로 Spotify의 기존 곡을 재생·정지했다. 종료 시 일시 정지 상태를 확인했다. SMSL 루프백과 MOTU 입력 1을 동시에 측정했으며 오디오 녹음은 저장하지 않았다.
- 일반 및 통화용 WASAPI 공유 모드 모두 MOTU 48kHz 입력은 열렸지만 설치된 AEC 콜백/참조 입력은 비활성이다. 따라서 시스템 전체 반향 제거는 검증 실패다. 스피커 EQ는 별도이며 실제 44.1kHz 콜백이 활성이다.
- 설치와 분리된 진단 프로세스에서 동일한 EchoStream 코어를 실행했다. SMSL 루프백은 Windows 변환으로 48kHz float stereo, MOTU 입력은 48kHz float stereo로 받았다. 첫 마이크 채널을 처리했다. 이 출력은 통화 앱이나 시스템 마이크로 전달되지 않는다.
- 180초 측정: 음악 재생 10~80초 중 참조 음량 > -45dBFS인 70개 1초 구간의 입력/출력 RMS 차이 중앙값은 **15.89dB**였다. 93초 이후 참조 < -100dBFS, 마이크 > -40dBFS인 45개 구간의 차이 중앙값은 **0.00dB**(범위 -0.33~0.69dB)였다.
- 이 값은 전체 입력/출력 음량 차이이며 순수 ERLE나 음성 명료도 점수가 아니다. 아이패드 음성의 정확한 시작/종료 라벨과 깨끗한 원본을 기록하지 않아, 음악과 음성이 동시에 나올 때의 음성 보존은 추가 검증이 필요하다. 음악 정지 시 루프백 패킷이 없어 참조 부족 카운터가 증가하며 원래 마이크를 통과시켰다.
- 측정 CSV: `measurements/aec-2026-09-20-independent-dsp.csv`. 독립 진단은 Windows 시스템 AEC 적용 성공을 뜻하지 않는다.
- Microsoft AEC 샘플에 맞춰 Windows 11에서 사용할 수 있는 v2 초기화 컨텍스트를 거부하던 코드를 수정했다. 장치/클래스 검증을 유지하고 v2 발견·잘못된 클래스·선택 장치 누락 회귀 테스트를 추가했다. 이 문제가 실제 미로딩의 원인인지는 아직 확인되지 않았다.
- 전체 CTest 5/5 통과 후, v2 변경에 대한 APO 계약 테스트도 통과했다. 제한된 크기의 생성 진단 로그를 추가했다. 새 DLL은 빌드/패키지에만 있으며 설치된 DLL에는 아직 반영되지 않았다.
- 오디오 서비스/장치 관리 서비스 재시작 및 새 DLL 적용을 위한 UAC 요청은 취소로 종료됐다. 재요청하지 않았으며 관리자 승인이 가능한 시점을 사용자에게 문의했다. 시스템 로딩 확인과 실제 시스템 출력 A/B 측정이 남아 있다.

참고: [Microsoft AEC APO 샘플](https://github.com/microsoft/Windows-driver-samples/blob/main/audio/sysvad/APO/AecApo/AecApoMfx.cpp).

## 2026-09-20: 이 PC의 미서명 설치 및 실제 EQ 처리

- 사용자 요청에 따라 LocalUnsigned 옵션을 추가했다. DisableProtectedAudioDG의 이전 값과 존재 여부를 설치 백업에 포함하며, 제거 시 함께 복원한다. 서명 손상은 허용하지 않는다.
- 장치 효과 키에 CreateSubKey 권한을 불필요하게 요청하던 설치 문제를 수정했다. 기존 키는 SetValue 권한만 사용하며 장치 키의 ACL/소유권은 바꾸지 않는다. 초기 실패의 COM/보호 설정은 원복 후 재설치했다.
- 원복 시 한 항목의 실패가 나머지 항목 복원을 막지 않도록 변경했다. 제한된 HKCU 테스트 키에서 값 설정/제거, 보호 설정의 원래 부재/0/1 복원, 서명 정책을 PowerShell 5.1과 7에서 검증했다.
- 실제 audiodg가 COM 집계로 APO를 생성하는데 기존 클래스 팩터리가 이를 거부하는 문제를 확인했다. 제어 IUnknown과 내부 IUnknown을 분리하고, 배포 DLL의 집계 생성/COM 객체 동일성/참조 수 회귀 테스트를 추가했다.
- 실제 SMSL 공유 모드 스트림에서 EQ 콜백 활성, 44,100Hz, 오류 0을 확인했다. MOTU 48,000Hz 캡처 스트림 생성은 성공했지만 AEC 콜백은 확인되지 않았다. 무음 재생과 저장하지 않는 캡처로 확인했으며 실제 방의 EQ 응답/반향 감소 측정은 아니다.
- 복원한 L/R 필터는 Resources/EQ와 E:/Speaker에 보관했고 설치된 사본은 ProgramData/SoundControl에 있다.
- 실제 Netflix/Spotify 재생은 이번 검증에 포함되지 않았다.

아래 기록의 미서명 설치 차단/필터 누락은 당시 상태이며, 위 항목으로 갱신되었다.

## 2026-09-20: WinUI 3 전환

Windows 로컬 저장소에서 C# / XAML WinUI 3 앱을 새로 빌드했다. Windows App SDK 1.8.260804001, .NET SDK 8.0.425를 사용했고, Windows App SDK와 .NET 런타임을 포함해 게시했다. XAML PRI 리소스가 게시 폴더에 포함되는 것도 확인했다.

- 네이티브 코어·APO·설정 브리지·초기화 도구를 Windows에서 LLVM-MinGW와 Microsoft Windows SDK 헤더로 새로 빌드했다. 포터블 빌드 명령은 Scripts/build-windows-portable.ps1에 기록했다.
- CTest **5/5 통과**: 스트리밍, 코어, 마이크 상태, APO 계약, WinUI 브리지.
- 브리지 테스트는 공유 설정 저장과 읽기, EQ/AEC 개별 토글, 실제 필터 값 반환, 유효한 파일 가져오기, 무효한 파일 거부와 기존 파일 보존, 잘못된 endpoint 거부를 검증했다. 임시 설정 폴더만 사용했다.
- 설치 복원 PowerShell 테스트 통과.
- Computer Use로 실제 WinUI 창 실행, SMSL/MOTU 인식, 양방향 휠 스크롤, 사운드/처리 상태 전환, 초기 EQ 파일 누락 안내를 확인했다.
- 닫기 버튼으로 창을 숨겨도 같은 프로세스가 유지되며, EXE를 다시 실행하면 새 창·프로세스를 중복 생성하지 않고 기존 창을 복원하는 것을 확인했다.
- 수동 GDI 그리기·자식 HWND 스크롤 코드는 제거했다. ScrollViewer가 합성 스크롤을 수행하며, 상태 변경이 없는 장치 목록과 프로필을 재바인딩하지 않는다.
- 작은 창의 카드 재배치는 구현했지만 모든 창 크기·DPI·고대비·스크린 리더 검증 및 프레임 단위 깜빡임 측정은 하지 않았다.
- MSVC 빌드와 원격 GitHub CI는 이 PC에서 실행하지 않았다. CI에는 .NET SDK 설치와 WinUI 게시 단계를 추가했다.
- 미서명 상태이며 초기 EQ 파일도 없다. 실제 장치 효과 설치·audiodg 처리 검증은 여전히 미완료다.

아래는 이전 이식 시점의 기록이다.

2026-09-19, Windows 11 Pro build 26200에서 검증했다.

## 통과

- 공통 코어를 Linux에서 빌드하고 기존 코어·마이크 상태·신규 스트리밍 테스트 3개를 실행했다.
- Windows x64 EXE/DLL을 빌드하고 **Windows에서 직접 실행한 CTest 4/4가 통과**했다: 코어, 마이크 상태, 스트리밍, APO 계약 테스트.
- 기존 AEC 감소량/음성 보존 회귀 기준은 낮추지 않았다.
- Windows APO 테스트는 실제 패키지 DLL의 로딩·COM 생성/해제, 형식 협상, EQ 결과, 1024프레임 지연, 입력 1 선택, Unicode 파일 교체, 효과 상태 이벤트를 포함한다. 장치 등록은 하지 않는다.
- 설치 복원 테스트는 임시 HKCU 키에서 문자열, 다중 문자열, DWORD, 새 키 제거, 직렬화 후 복원 및 재실행을 검증했다.
- Fluent 디자인 변경 전 네이티브 설정 창의 생성, 응답 및 정상 종료를 확인했다.
- `git diff --check` 통과.

## 빌드 환경

이 PC에는 MSVC/Windows SDK가 설치되어 있지 않아 임시 MinGW-w64 도구체인과 Microsoft 공개 Windows SDK 인터페이스 헤더로 크로스 빌드했다. 테스트 EXE는 Linux 에뮬레이션이 아닌 실제 Windows에서 실행했다. 임시 도구체인/SDK 헤더는 리포지토리에 포함하지 않았다.

MSVC x64용 CMake/PowerShell 빌드와 GitHub Actions 워크플로를 추가했지만, 이 세션에서는 MSVC 및 원격 CI 실행을 검증하지 않았다. macOS 실행 검증도 하지 않았다.

## 합성 신호 성능

- 타임스탬프와 480프레임 패킷을 사용하는 스트리밍 테스트: 약 **40.9dB** 반향 감소.
- 기존 Windows AEC 벤치마크: 단일 코어 환산 **2.319%**, p99 **470.2μs**, 선형 감소 29.183dB. 이 CPU 결과는 벤치마크에 표시된 기존 1.5% 목표보다 높다.
- 마이크 스트리밍 계층의 고정 추가 지연: **21.33ms**. EQ에는 추가 버퍼 지연이 없다.

실제 스피커/방/마이크 측정값이 아니며, 드라이버 지연·오디오 스케줄링 지연은 별도다.

## 미완료: 실제 오디오 장치 적용

SMSL USB DAC 및 MOTU In 1-2 endpoint가 존재하는 것을 확인했지만 설치 사전 점검에서 `SoundControlAPO.dll`의 서명이 `NotSigned`로 확인되어 설치가 중단되었다.

- 장치 효과 레지스트리 및 Windows 오디오 보안 설정은 변경하지 않았다.
- 실제 audiodg 로딩, 스피커 EQ 측정, 실제 마이크 반향 감소, 통화 앱 호환성, 실장치 재연결은 아직 검증하지 않았다.
- 다음 검증에는 Protected Audio의 요구 조건을 충족하는 서명된 APO가 필요하다. 일반 프로세스에서 DLL 로딩 테스트가 통과한 사실은 audiodg의 수용을 보장하지 않는다.

## Fluent UI 변경 후 검증 범위

새 UI의 Windows x64 빌드는 통과했다. 밝게/어둡게 테마, 고대비 색상, DPI 재배치, 작은 창의 스크롤, 네이티브 입력 컨트롤을 구현했다. Windows 자동화 도구가 WSL 작업 경로를 거부하여 실제 창 캡처와 클릭 검증을 진행하지 못했다 (`sandboxCwd is not a local file URI`). 따라서 한글 렌더링, 각 배율의 시각적 완성도, 키보드/스크린 리더 접근성 검증은 완료로 표시하지 않는다.
